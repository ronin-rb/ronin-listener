#!/usr/bin/env ruby

require 'ronin/listener/dns'

Ronin::Listener::DNS.listen(<%= @domain.inspect -%>, host: <%= @host.inspect -%>, port: <%= @port -%>) do |query|
  puts "Received query for #{query.type} #{query.label} from #{query.source}"
end
